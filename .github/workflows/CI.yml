name: CI

on:
  push:
    branches:
      - main
      - dev
      - qa
      - prod

  pull_request:
    branches:
      - main
      - dev
      - qa
      - prod

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3
